name: CI

on: [push]

jobs: 
  build:
  
    runs-on: ubuntu-18.04
    
    steps:
     - uses: actions/checkout@v1
     - name: run a one-line script
       run: echo Hello world !!!
       shell: bash
